let hueSlider;

export default {
    hueSlider: {
        svgMargin: 20,
        radius: 140,
        trackThickness: 8,
        set: function(that){ hueSlider = that },
        get: function(){ return hueSlider }
    },
    triangleSlider: {
    }
}